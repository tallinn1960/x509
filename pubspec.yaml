name: x509
description: Dart library for parsing and working with X.509 certificates.
version: 0.2.7
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  asn1lib: ^1.0.0
  quiver: ^3.0.0
  crypto_keys:
    git:
      url: https://github.com/tallinn1960/crypto_keys.git
      ref: develop


dev_dependencies:
  test: ^1.0.0
  pedantic: ^1.9.0

